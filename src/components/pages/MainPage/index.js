
import React from 'react'
import { PageTemplate, ErrorBoundary } from 'components'

const MainPage = () => (
  <PageTemplate>
    <ErrorBoundary>
      MAIN
    </ErrorBoundary>
  </PageTemplate>
)

export default MainPage
